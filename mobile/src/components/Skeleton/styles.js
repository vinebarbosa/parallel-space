import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {
    width: 90,
    height: 90,
    backgroundColor: '#292A2D',
    borderRadius: 18,
    marginHorizontal: 8,
    marginVertical: 9,
    overflow: 'hidden'
  }
})
